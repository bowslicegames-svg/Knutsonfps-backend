services:
  - type: web
    name: knutsonfps-backend
    env: node
    region: frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      PORT: 8080
